# Flick release signing

This keystore signs public GitHub Release APKs so sideload updates share one certificate.

Password and alias are in `signing.properties` (intentional for FOSS sideload builds).
